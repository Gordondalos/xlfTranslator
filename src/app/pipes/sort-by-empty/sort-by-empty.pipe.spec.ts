import { SortByEmptyPipe } from './sort-by-empty.pipe';

describe('SortByEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
